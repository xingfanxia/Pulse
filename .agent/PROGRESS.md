# CLP progress ledger

Contract: Docs/clauth/GOAL-PROMPTS.md · design: Docs/clauth/PLAN.md

## CLP-0 — planned 2026-09-02
- Fork cloned at ~/projects/devtools/Pulse, upstream remote added (2ab2948).
- Baseline (2026-09-02): Swift 6 build clean, 0 warnings; localization 249 keys; no test target.
- Next: Task 0 (re-run baseline, understanding receipt), then CLP-1.

## Task 0 — baseline re-run 2026-09-03T05:40Z (HEAD e32e8e2)
- `git rev-parse --short upstream/main` = 2ab2948 ✓
- `swift package clean` + Swift 6 build: `Build complete! (6.88s)`, `grep -c warning:` = 0 ✓
- `Scripts/check-localization.sh`: 249 keys ✓
- Real-machine baseline snapshot (read-only): profiles = [ax-backup, ax-cl, ax-code-bk, ax-codex-cl, ax-codex-dev0, ax-codex-xfx, ax-main] (7); active_profile = ax-main; active_codex_profile = ax-codex-dev0; `ls ~/.clauth/profiles | sort` = ax ax-backup ax-cl ax-code-bk ax-codex-cl ax-codex-dev0 ax-codex-xfx ax-main cl-ax p xfx xfx-backup xfx-cl xfx2 (14); clauth 0.13.1; com.clauth.proxy loaded.
- `~/Library/LaunchAgents/com.pulse.launch-at-login.plist`: none ✓

### 理解回执
- 目标：Pulse 的悬浮 rail 成为每个 clauth 账号一环的常驻面，并吸收 ccsbar 的全部控制面；Pulse 零 token，只读 status.json + socket。
- 顺序：CLP-1 只读环（ClauthStatus/Watcher/Mapping/Visibility/FetchGuard + 7 账号 fx- fixture + 沙箱裸启动 + 网络/重启/stale/丢弃反向验证）→ CLP-2 右键菜单 spike + ClauthCLI 唯一 spawn 门 + DaemonClient + settle ladder 三腿 + 卡片脚注 → CLP-3 设置面板 + 链编辑十命令 + proxy/rolling-token + PARITY.md + 安装 + 自动更新关 + 只读冒烟 + 真机快照断言 → 收尾包 CLP-CLOSEOUT。
- 最大风险：(1) 对真实 daemon 误发写命令 —— 所有 spawn 经 ClauthCLI，沙箱下无 PULSE_CLAUTH_BIN 即拒绝，dev 全程 PULSE_CLAUTH_HOME 指向 fixture；(2) 逻辑漏进上游文件超 hook 预算 —— 一切在 Sources/Pulse/Clauth/；(3) 裸启动写 LaunchAgent —— 先 `defaults write Pulse settings.launchAtLogin.decided -bool YES`；(4) `enabledAccounts`/`restored()` 把 clauth id 剪掉 —— ClauthVisibility 接管 `shownAccounts`，绝不碰 enabledAccounts；(5) 评分器冻结在 56754d6，任何改动即失败。

## CLP-1 — clauth accounts on the rail (read-only) — DONE 2026-09-03
Files: `Sources/Pulse/Clauth/{ClauthStatus,ClauthMapping,ClauthLiveness,ClauthVisibility,ClauthFetchGuard,ClauthWatcher}.swift`, `Tests/PulseTests/*` (72 XCTest), `Tests/PulseTests/Fixtures/status.json` (7 `fx-` profiles, 3 claude / 4 codex), hooks: AppDelegate 2 · AppSettings 8 · FloatingUsagePanelView 2 · SettingsView 1 · UsageStore 5 = **18 / 32**.
Evidence (transcript; images `Docs/clauth/evidence/clp1/`):
- `CLP_HOOK_BUDGET=32 Scripts/clauth-verify.sh` → exit 0 — build 0 warnings · `Executed 72 tests, with 0 failures` · localization 251 keys · hook budget 18/32 · gates 5/6 `none` · gate 7 `untouched`.
- Sandbox bare launch (`PULSE_CLAUTH_HOME=<scratch>/clp-sandbox`, recipe + ticker in DECISIONS): exactly 7 rings (12 / 100 / 5 / 33 / 100 / 15 / 100) in roster order; hover cards titled `fx-main Usage` and `fx-codex-xfx Usage`; quit + relaunch → the same 7; `~/Library/LaunchAgents/com.pulse.launch-at-login.plist` absent before and after every run.
- Network: `lsof -nP -i -a -p <pid>` ×3 within 60 s all empty (22:58:08 / :28 / :48); cursor positive control: lsof 0/300 yet the fetch completed → `lsof` is blind to Network.framework flows (DECISIONS). `nettop` oracle: sandbox run — process never appears (0 samples with bytes); cursor run — 7096 B in / 3237 B out in the first second. Two-provider config restored and read back.
- Stale 反向: `generated_at` −2 h + `touch -t` −2 h → card footnote "As of 1 hour ago"; fixture restored + ticker → footnote gone. XCTest `testTwoHourOldFileReadsStaleThenRecovers`, `testStaleGeneratedAtWithFreshMtimeIsStillLive`.
- Drop 反向: fx-main `5h` → `3d` → card shows only the two Weekly rows, ring reads 12 % (7d, never the scoped fable window). XCTest `testFixtureWindowRelabelledThreeDaysDisappearsFromTheReading`, `testDefaultPinForAReadingFallsToTheOtherUnscopedWindowNeverAScopedOne`.
- Inactive accounts hidden by default (AX mid-run): fx-code-bk tier → `free` → 6 rings; restored → 7. XCTest `testInactiveIsCancelledOrFreePlanOrBrokenLogin`, `testInactiveSetNeverHoldsTheActiveSlot`, `testInactiveAccountsAreHiddenByDefaultAndShownWhenTheFlagIsOff`, `testInactiveProfilesLeaveTheRailWithoutLeavingTheRoster`.
- Graders: `git diff 56754d6 -- Scripts/clauth-verify.sh Scripts/clauth-hook-budget.sh Makefile` → empty.
Next: CLP-2 — step 1 the right-click spike (`.contextMenu` on a ring, glass off / on, screenshots → DECISIONS), then `ClauthCLI` / `ClauthDaemonClient` / `ClauthSwitchMachine` / `ClauthRingMenu` / `ClauthCardFooter`.

## CLP-2 — switch from the ring, the card knows the chain — DONE 2026-09-03
Files: `Sources/Pulse/Clauth/{ClauthCLI,ClauthDaemonClient,ClauthSwitchMachine,ClauthActions,ClauthRingMenu,ClauthCardFooter}.swift`; watcher gains `client`/`switches`/`actions` and is `@Observable`; tests `Tests/PulseTests/{FakeClauthDaemon,ClauthCLITests,ClauthDaemonClientTests,ClauthSwitchMachineTests,ClauthSwitchControllerTests,ClauthRingMenuTests,ClauthCardFooterTests,ClauthActionsTests}.swift`. Hooks: UsageDockItem `.modifier(ClauthRingMenuModifier)` 1 · UsageDetailCard footer 1 + footnote 2 → **23 / 40** total.
Evidence (transcript; images `Docs/clauth/evidence/clp2/`):
- Spike: `.contextMenu` presents on the non-activating panel with glass OFF and ON (`spike-contextmenu-glass-{off,on}.jpg`) — no `sendEvent` fallback needed (DECISIONS).
- `CLP_HOOK_BUDGET=40 Scripts/clauth-verify.sh` → exit 0 — build 0 warnings · `Executed 117 tests, with 0 failures` · localization 293 keys · hook budget 23/40 · gates 5/6 `none` · gate 7 `untouched`; grader diff vs 56754d6 empty.
- Settle ladder (XCTest over a Unix-socket `FakeClauthDaemon`): `settle: fake socket recorded [["profile": fx-cl, "cmd": switch]]; active_profile flipped to fx-cl; phase confirmed(target: "fx-cl", viaCLI: false)`; the codex slot untouched.
- 反向 wrong harness: `reverse: wrong-harness flip → active_codex_profile=fx-cl, active_profile=fx-main, phase failed(reason: "the switch didn’t take — the daemon may be busy")` — stayed pending through 3.5 s, failed at the 6 s pending timeout, never confirmed.
- Arming: `arming: idle after 5 s, fake socket log []` (has_live_session=true on the current claude, no confirm surface); a confirmed arm dispatches exactly one `switch`; a codex switch never arms; the dead-daemon leg confirms via the CLI exit code; a daemon rejection never reaches the CLI.
- ClauthCLI sandbox refusal: `PULSE_CLAUTH_HOME` set without `PULSE_CLAUTH_BIN` ⇒ `.unavailable(.sandboxed)` for `clauth` AND `/bin/launchctl`, the injected spawn closure never called; with the shim every program is handed to the shim as `[program] + args`.
- Decision table: active row has no Switch; `auth_status=broken` leads with Re-authenticate; third-party rows have no reauth/capture; every row ends with Hide from rail.
- Codex verdict line: bare reason + 40 % live window → "fx-x is rate-limited", window not exhausted, `UsageTint.isSpent == false`; bare reason + 100 % live window → "hit its weekly window" (+ reset); named verdicts; lapsed windows silent.
- Sandbox smoke (bare build, fake socket at `/tmp/clp-sb/clauthd.sock`, shim as `PULSE_CLAUTH_BIN`): right-click fx-cl → `Switch to fx-cl / Refresh / Re-authenticate… / Rename… / Hide from rail` (`ring-menu-fx-cl.jpg`); Switch → NSAlert (`arming-alert.jpg`) with the card reading "Waiting for confirmation…" (`card-waiting-for-confirmation.jpg`); Switch clicked → fake socket log `{"cmd":"switch","profile":"fx-cl"}`, status.json `active_profile fx-cl` / `active_codex_profile fx-codex-dev0`, shim log empty (socket path taken), rail reorders with fx-cl first (`rail-after-switch.jpg`); fx-codex-xfx card shows `chain #2 · switches at 95%`, "hit its weekly window — auto-switch rotates at the session boundary · resets in …", `1 free reset banked` (`card-codex-verdict-banked.jpg`). LaunchAgent plist absent throughout.
Next: CLP-3 — settings pane (`SettingsPane.clauth`), chain editor over the ten socket verbs, proxy row (`PULSE_CODEX_HOME`), rolling-token row, add/rename/delete, `ClauthAccountPane` for `clauth:` slots, PARITY.md, bundle + install + auto-updates off, read-only real-daemon smoke, snapshot oracle.

## CLP-3 — the control pane, ccsbar parity — DONE 2026-09-03
Files: `Sources/Pulse/Clauth/{ClauthProxyMode,ClauthChainEdit,ClauthSettingsPane,ClauthChainEditor,ClauthProxyRow,ClauthAccountPane}.swift`, `ClauthActions` (+ the ten chain verbs, add/delete/setup-token/feed/proxy), `Docs/clauth/PARITY.md`; tests `ClauthProxyModeTests`, `ClauthChainEditTests`, `ClauthPaneTests`, `ClauthActionsTests` (+3). Hooks: SettingsView 11 (pane case/title/symbol, sidebar row, detail case, icon switch, `accountPane` guard, `loadHistory` guard), AppDelegate +1 (`PULSE_OPEN_SETTINGS`) → **34 / 56** total.
Evidence (transcript; images `Docs/clauth/evidence/clp3/`):
- `CLP_HOOK_BUDGET=56 Scripts/clauth-verify.sh` → exit 0 — build 0 warnings · `Executed 141 tests, with 0 failures` · localization 395 keys · hook budget 34/56 · gates 5/6 `none` · gate 7 `untouched`; grader diff vs 56754d6 empty.
- PARITY.md: every `StatusModelActions` func, the `CodexProxyRow` switch and `SessionToken` install/replace/clear ticked with the landing commit; 5 rows "not done" with the reason (ccsbar's inspected-row state, its snapshot-render guard, and the sidecar-reading status/mis-fill lines Pulse never reads by design).
- Ten chain verbs end to end over the fake socket, in click order: `fallback_add, fallback_remove, fallback_move, set_threshold, set_last_resort, set_member_weekly, set_member_weekly(null), set_check_weekly, set_check_scoped, set_wrap_off, set_weekly_threshold` — each recorded JSON asserted literal against `socket.rs`.
- Shim argv contract (recorded, matches ccsbar `loginArgs`/`deleteArgs`/`setupTokenArgs`): `login --new fx-new` · `login --new fx-codex-new --codex` · `login --new fx-codex-web --codex --browser` · `login fx-main --setup-token --yes` (mint on stdin only) · `feed fx-main off` · `delete fx-backup --yes`; a real shim run records `clauth login fx-main / clauth login fx-main --setup-token --yes / clauth delete fx-backup --yes` with the stdin token absent from argv.
- Proxy toggle on the sandboxed bare build (`PULSE_CODEX_HOME=/tmp/clp-sb/codex`, driven through the accessibility tree): ON diff = `+model_provider = "clauth"` + the `[model_providers.clauth]` block; backup `config.toml.bak-pulse` equals the pre-edit file; shim log `/bin/launchctl bootstrap gui/501 …/com.clauth.proxy.plist`; OFF diff = the routing line gone, the block kept; real `launchctl list | grep com.clauth.proxy` = `25853 -15` before/after both; real `~/.codex/config.toml` mtime unchanged (Sep 2 02:15).
- clauth account pane: `ClauthAccountPane.actions` table has no sign-out / remove / rename; a source assertion pins the pane free of `"Sign out"`, `"Remove account"`, `settings.rename(`, `removeAccount(`, `AccountCredentialStore`.
- Install: `Scripts/bundle.sh` → `build.noindex/Pulse.app` → `cp -R` → `/Applications/Pulse.app` (1.0.4, `io.github.qunqin24.Pulse`); `open -a` → `ps` shows `/Applications/Pulse.app/Contents/MacOS/Pulse`; `defaults write io.github.qunqin24.Pulse SUEnableAutomaticChecks -bool false` → `defaults read` = 0.
- Real-daemon read-only smoke: `clauth 0.13.1`, `generated_at 2026-09-03T06:53:11+00:00`, 7 profiles; the installed app's Settings sidebar lists `ax-main, ax-backup, ax-cl, ax-codex-dev0, ax-codex-xfx, ax-code-bk, ax-codex-cl` (AX read) = the 7 published profiles; rail with hide-inactive off = 7 clauth rings (`installed-rail-all-seven.jpg`), card `ax-main Usage` with `active · not in chain · rolling token`; rail at the default = 3 rings (`installed-rail-default-inactive-hidden.jpg`).
- Snapshot oracle re-run 06:53:40Z: profile set, both active slots (ax-main / ax-codex-dev0) and the 14 profile dirs identical to the Task 0 baseline; no switch/rename/delete line stamped in `~/.clauth/daemon.log` inside the run window; `~/.claude/settings.json` mtime Sep 2 11:34 (untouched); no `com.pulse.launch-at-login.plist`.
Closeout: `Docs/clauth/CLP-CLOSEOUT-2026-09-03.md` (committed 02e7594; test-order flake fixed in 06f9aa1). CLP-4 (retire ccsbar) awaits AX.

## CLP-5 — rail semantics: labels, active cue, inner ring — DONE 2026-09-03 (final shape after AX's four follow-ups + the review round)
Files: `Sources/Pulse/Clauth/ClauthRail.swift` (`ClauthCaption`, `ClauthRailCaption`, `ClauthRingExtras`), `ClauthVisibility` (+ `railCaptions` / `innerRing` / `activity`, `showsActivity`), `ClauthRingMenuModifier` applies the extras to every ring, clauth pane "Rail" group; tests `ClauthRailTests` (7). Hooks +11 → **45 / 56**.
Final shape: outer ring = the WEEK (default pin `7d`), inner arc = the 5h window (single ring for weekly-only); label under each ring = the email's local part (profile name one switch away), the active account's in a solid accent capsule; upstream's working mark off by default (Off / Active / All). Review round folded (13 findings, DECISIONS). Evidence: `Docs/clauth/evidence/clp5/rail-captions-inner-ring-active-capsule.jpg` (sandbox), `installed-rail-email-captions.jpg` (real: `xingfanxia` · `ax0xdev` · `xingfanxia`); verify exit 0, 153 XCTest, hooks 49/56. Install-time leak found and fixed: the bundle imports the bare `Pulse` domain once (DECISIONS) — `defaults delete Pulse` before installs from now on. Then: frosted dark surface for rail + card + sliver (`ClauthFrostedSurface`, 2 hook lines → 51/56), and the sliver's alert colour from the active slots only; the sliver accent-washed with a bright edge and a configurable width (default 10 pt) — `rail-frosted-dark.jpg`, `card-frosted-dark.jpg`, `sliver-frosted-neutral.jpg`, `sliver-frosted-alert.jpg`. Hooks 53/56.
