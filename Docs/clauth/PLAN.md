# CLP — Pulse × clauth: the rail absorbs ccsbar

Status: **CLP-1 → CLP-3 shipped 2026-09-03** (one `/goal` run; ledgers under `.agent/`, evidence under `Docs/clauth/evidence/`, closeout `CLP-CLOSEOUT-2026-09-03.md`, parity `PARITY.md`) · CLP-4 human-gated. Planned 2026-09-02 after two adversarial review rounds (42+37 findings, 25+18 upheld, all folded in).

## Objective

Pulse's floating rail becomes the one always-on surface for every account clauth manages — **every claude profile and every codex profile, one ring each** — and gains clauth's full control face (switch, chain, thresholds, proxy mode, rolling token, add/rename/delete) so ccsbar can retire. Pulse holds **zero tokens** for these accounts: clauth's daemon is the data plane (`~/.clauth/status.json` + `~/.clauth/clauthd.sock`), Pulse renders and sends commands.

Why this shape (decided 2026-09-02, AX): the menu-bar label ccsbar owns has a 12-character budget — N accounts × 2 harnesses cannot fit. A ring per account on a rail is the correct display, and Pulse already built it (floating `NSPanel`, edge docking, sliver fold, multi-display memory, ~2.5k LOC of edge cases). Porting that into ccsbar means maintaining a fork of it by hand; consuming it as a fork means upstream fixes arrive by merge.

## The one architectural rule — all logic in new files, hooks are one-liners

Upstream `qunqin24/Pulse` was born 2026-08-30 and landed 100 commits in four days; `SettingsView.swift` (1434 LOC) changed 35 times in that window, `UsageStore.swift` 19, `AppSettings.swift` 18, `UsageDockView.swift` 14, `UsageDetailCard.swift` 13 (`git log --since='60 days ago'`, 2026-09-02). Anything written *inside* those files is a merge conflict every week. Therefore:

- Every clauth file lives under `Sources/Pulse/Clauth/` (SwiftPM includes subdirectories of the target path — verified with a scratch package 2026-09-02). Upstream never touches that folder.
- Upstream-owned files receive **call-site hooks only**: an appended array, a `case`, a modifier, a guard, a stored property with a `didSet` that calls into `Clauth/` — each ≤3 lines. The total is enforced by `Scripts/clauth-hook-budget.sh` (added lines in upstream-owned Swift files; `.strings`, `Package.swift`, `Docs/`, `.agent/` exempt). **Budgets: CLP-1 32 · CLP-2 40 · CLP-3 56.**
- **The graders are frozen.** `Scripts/clauth-verify.sh` and `Scripts/clauth-hook-budget.sh` were written and committed by the authoring session BEFORE the goal fires; the executor never edits them (`git diff <kickoff-sha> -- Scripts/clauth-*.sh` must be empty at every milestone — it is one of the acceptance lines).
- `main` tracks `upstream/main` by **true merge**, weekly or before each milestone; never rebased, never force-pushed. Our commits carry a `clp(N):` prefix so `git log --grep` separates the two lines.
- No release tags on the fork (a tag drives upstream's Sparkle release workflow); the app is installed from a local `Scripts/bundle.sh` build.

## Grounded facts (verified 2026-09-02 unless marked)

- Pulse `main` = upstream `2ab2948` ("Offer 1.0.4 to Sparkle"), fork 0 ahead / 0 behind. Clean build: `swift package clean && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift build -Xswiftc -swift-version -Xswiftc 6` → `Build complete!`, **0 warnings**, 7s. `Scripts/check-localization.sh` → "249 keys". No test target, no linter (`CLAUDE.md`). A `testTarget` can `@testable import` the SwiftUI `@main` executable target (scratch package, tools 6.0, Swift 6.3.3).
- Toolchain: `xcode-select -p` = `/Applications/Xcode.app/Contents/Developer`. `#Preview` blocks need Xcode — never strip them (`CLAUDE.md`).
- **Account identity** (`MonitoredAccount.swift`): `AccountKey(provider, slot)`; first account id = bare provider raw value, extras `provider#slot`. `AppSettings.allAccounts` = primaries + persisted `extraAccounts`. **Visibility is a second gate**: the rail draws `settings.shownAccounts` = `orderedAccounts.filter(isEnabled)` (`AppSettings.swift:612`), `isEnabled` = `enabledAccounts.contains(id)` (`:598`), and `restored()` rebuilds `enabledAccounts` at launch as *providers ∪ (stored ∩ persisted ExtraAccount ids)* (`:497,:555`) — any other id is dropped and the pruned set is written back (`:556`). `enabledAccounts.didSet` refuses an empty set (`:111-118`); `restored()` re-enables ALL providers when both halves come out empty (`:553`). `PanelMetrics.makeRoom(for: allAccounts.count)` (called from `extraAccounts.didSet` `:59` and `restored()` `:594`) sizes the panel window — a roster change that skips it clips the rail.
- **Rail**: `RailEntry{usage, headline, isRunning, isRefreshing, tint, elapsed, showsRemaining}` (two memberwise call sites: `FloatingUsagePanelView.swift:190`, `UsageDockView.swift:705` preview) → `UsageDockItem` → `UsageRingView`. `DockLayout.itemHeight` feeds the AppKit frame AND the hit testing (`UsageDockView.swift:61,110-175`) — a second text line under the % is NOT a one-line change. No `contextMenu` in the target.
- **Panel**: `[.borderless, .nonactivatingPanel]`, `canBecomeKey = false` (`FloatingPanelController.swift:100,295`); `sendEvent` intercepts `.leftMouseDown/Dragged/Up` and passes everything else to `super` (`:356-361`). `CLAUDE.md` documents that SwiftUI `.onHover` does not work on this panel and Liquid Glass swallows input — right-click presentation is unverified (see CLP-2 spike).
- **Settings**: `SettingsPane {general, account(AccountKey), about}`; sidebar sections Panel / Accounts / Application; `accountPane` renders Sign out (`SettingsView.swift:848`), a rename field bound to `settings.rename` (`:1039`, a no-op for non-`ExtraAccount`s), and "Remove account" → `AccountCredentialStore.set(nil)` + `removeAccount` (`:1048-1053`).
- **Usage model**: `ProviderUsage{account, windows:[UsageWindow], observedAt, state(live|stale|unavailable), plan, creditBalance}`; `UsageWindow{id, kind(fiveHour|weekly|spend|monthly|other), scope, usedFraction, windowSeconds, resetsAt, reportsLength, isExhausted}`. Card footnote for `.stale` = "As of <relative>" / "Reading may be out of date" (`UsageDetailCard.swift:173-181`).
- **UsageStore.usage is `private(set)`** — an extension in another file cannot write it; `applyClauth` is a 2-line method INSIDE `UsageStore.swift`.
- **Rail headline** = `usage.headlineWindow(preferring: settings.pinnedWindow(for:))` = the window with the MAX `usedFraction` when nothing is pinned (`ProviderUsage.swift:269-272`, `FloatingUsagePanelView.swift:190`) — a maxed `7d fable` would paint the ring red unless the headline is steered.
- **`Provider` has 12 cases** and `restored()` enables every installed / no-setup provider (`AppSettings.swift:519-538`); hiding two primaries leaves the others (Cursor, Antigravity, …) on the rail and fetching. The Settings "Order" rows subtitle "Not shown" from `settings.isEnabled` (`SettingsView.swift:363`); `row(_:)`'s icon `switch` is exhaustive over `SettingsPane` (`:112-121`).
- **First-launch side effects of the BARE build**: `AppDelegate.swift:46` → `LoginItem.applyDefaultOnFirstRun()` writes `~/Library/LaunchAgents/com.pulse.launch-at-login.plist` (RunAtLoad, ProgramArguments = the `.build` binary) unless UserDefaults `settings.launchAtLogin.decided` is already true — and launchd would then start that binary WITHOUT the sandbox env, pointed at the real `~/.clauth`. `StatusLineHook.install()` edits `~/.claude/settings.json` on user action only (alert / Settings toggle / CLI flag).
- **Installed app**: bundle id `io.github.qunqin24.Pulse`; `Scripts/bundle.sh` bakes `SUFeedURL` = upstream's appcast and `SUEnableAutomaticChecks = true`; `VERSION` = `1.0.4` = upstream's tip. **Upstream's next release would replace the installed fork with vanilla Pulse** unless automatic checks are switched off (`defaults write io.github.qunqin24.Pulse SUEnableAutomaticChecks -bool false`). Gate 7 forbids changing the feed here — CLP-4 item.
- **ccsbar's freshness predicate** is `isStale = fetchStatus != nil && fetchStatus != "Fresh"`; clauth's `stale` flag is narrower (a distrusted deep-slot RateLimited only). Live feed 2026-09-02: two claude profiles read `fetch_status: RateLimited, stale: false`.
- **Localization**: every user string via `String.localized(...)` + entries in `Sources/Pulse/Resources/{en,zh-Hans}.lproj/Localizable.strings`; `Scripts/check-localization.sh` fails on a missing key.
- **Sparkle** is inert in a bare `swift build`/`swift run` (no bundle → `CLAUDE.md`); only the bundled app checks the appcast. Sandbox smoke runs the BARE build (also keeps UserDefaults in the `Pulse` domain, not the installed app's — `CLAUDE.md`).
- **clauth feed** (`~/.clauth/status.json`, schema 1; contract: clauth `docs/ccsbar/DESIGN.md`; live daemon is clauth **0.13.1 fork main** — the pre-#59 spelling). Top level: `schema, generated_at, active_profile, active_codex_profile, fallback_chain, codex_fallback_chain, wrap_off` (**one global value — there is no `codex_wrap_off`** on the deployed daemon; PR #69 adds one upstream, decode-if-present), `weekly_switch_threshold, burn_aware, forecast, pending_switch, last_switch, last_error, refresh_interval_ms, clauth_version`. Per profile: `name, active, harness(claude|codex), provider(anthropic|openai|<third-party>), base_url, tier, account_email, has_live_session, auth_status(ok|expiring|broken|unknown), fetch_status(Fresh|Cached|…|null), stale(bool), fetched_at, next_refresh_at, auto_start, bell_threshold, fallback{position,threshold,armed,last_resort,member_weekly?,check_weekly?,check_scoped?}, windows[{label:"5h"|"7d"|"7d <model>", utilization_pct, resets_at}], third_party{available,…}, session_feed` (**live spelling**) / `rolling_token` (post-#59 spelling — decode both, prefer `rolling_token`, exactly ccsbar `DaemonStatus.swift:227-258`), `codex_snapshot_at, codex_rate_limit_reached, codex_reset_credits`. Published roster as of 2026-09-02: **7 profiles (3 claude / 4 codex)**; `ls ~/.clauth/profiles` shows 14 dirs — disabled profiles are not published.
- **clauth socket** `~/.clauth/clauthd.sock`, newline-delimited JSON. Wire authority: clauth `src/daemon/socket.rs` (dispatch arm) with `docs/ccsbar/DESIGN.md` § socket as the doc (all 14 verbs documented as of clauth `fa29c21`): `snapshot, switch, refresh, fallback_add/remove/move, set_threshold, set_last_resort, set_member_weekly` (**`value: null` or absent CLEARS**), `set_check_weekly, set_check_scoped, set_wrap_off` (global), `set_weekly_threshold, rename`. CLI-only verbs (ccsbar shells `clauth`): `login <name> [--codex] [--browser] [--new]`, `login <name> --setup-token --yes` (rolling token, token on stdin), `delete <name>`, `rolling-token <p> on|off`; `clauth` reads `~/.clauth` unconditionally (`src/profile.rs:932`, override is `#[cfg(test)]` only) — **no env var redirects the CLI**. Proxy mode = `~/.codex/config.toml` edit + `launchctl bootstrap gui/<uid> ~/Library/LaunchAgents/com.clauth.proxy.plist` (ccsbar `CodexProxyMode.swift:103-136`, paths hard-coded).
- **Reference implementation to port** (ours, `~/projects/devtools/ccsbar/Sources/CCSBarKit/`): `DaemonClient.swift` 667 (socket + CLI fallback `switchTo` → `shellClauth` when the socket is unreachable `:150-163`; `clauthBinary()` probes `/opt/homebrew/bin`, `/usr/local/bin`, `~/.cargo/bin` `:448-455`), `SwitchMachine.swift` 130 + `StatusModelSwitch.swift` 150 (settle ladder incl. the CLI-confirm leg and the live-session arm), `LivenessLadder.swift` 34 (`.live <5s / .syncing <15s / .dead`, over `min(generated_at age, status.json mtime age)`), `Exhaustion.swift` 55 (threshold **99.5**; a cap binds only while `resets_at` parses AND is in the future — missing/unparseable counts as spent; the scoped per-model window never contributes; "week spent" outranks "5h spent"), `ChainEdit.swift` 245, `CodexProxyMode.swift` 159, `SessionToken.swift` 163 (its status half reads the sidecar file — see rails: Pulse ports only the INSTALL/CLEAR verbs and reads the flag from status.json), `MachineTokens.swift` 272, `StatusModelActions.swift` 525. `Tests/CCSBarKitTests/` (230 XCTest) is the port's characterization suite.

## Milestones

### CLP-1 — clauth accounts on the rail (read-only)

Deliverable: launch the bare build with `PULSE_CLAUTH_HOME=<sandbox>` → one ring per fixture profile, both harnesses, numbers identical to the fixture, no network request for them, and the same rings after quit + relaunch.

- `Clauth/ClauthStatus.swift` — `Codable` mirror of status.json (decodeIfPresent everywhere; `rolling_token` ?? `session_feed`; schema ≠ 1 → unsupported). Port from ccsbar `DaemonStatus.swift` minus its view helpers.
- `Clauth/ClauthWatcher.swift` — mtime-polled (2s) reader of `${PULSE_CLAUTH_HOME:-~/.clauth}/status.json`; publishes the roster into `AppSettings.clauthAccounts` and readings into `UsageStore.applyClauth`. Owned and started from `AppDelegate` (one stored property + one `start()` next to `store.start()` — 2 hook lines).
- `Clauth/ClauthMapping.swift` — pure: profile → `AccountKey(.claudeCode|.codex, slot: "clauth:<name>")` (`harness` absent ⇒ claude; third-party providers → `.claudeCode` with `plan` = provider); windows → `UsageWindow`: `5h`→`.fiveHour`/18000s, `7d`→`.weekly`/604800s, `7d <model>`→`.weekly` scope=`<model>`; **any other label is DROPPED** (Pulse's own rule: never invent a length); `isExhausted` = ccsbar's three-part rule verbatim and NOTHING else (99.5 · reset still ahead, missing = spent · scoped never) — the codex verdict is a card LINE (CLP-2), never a ring color, exactly as ccsbar keeps them apart; `state` = `.stale` when `stale == true` OR `fetch_status != "Fresh"` OR the liveness ladder reads `.dead` (ccsbar's `isStale`), else `.live`; `observedAt` = `fetched_at`; `plan` = tier; codex `creditBalance` = "1 reset banked" when `codex_reset_credits > 0`; `defaultPin(for:)` = `5h` for claude, `7d` for codex — the rail headline must never be a scoped window.
- `Clauth/ClauthVisibility.swift` — owns the visibility state Pulse's `enabledAccounts` cannot hold: `clauthHiddenAccounts: Set<String>` (our UserDefaults key, default empty = every clauth profile shown) and `clauthHidesPrimaries: Bool` (default true once a feed is seen; user-toggleable in the clauth pane). One hook replaces the body of `shownAccounts`: `ClauthVisibility.shown(orderedAccounts, settings: self)` — the UNFILTERED ordered list, so the user's drag order (which may interleave clauth ids among primaries) survives; the function applies both gates in place (`isEnabled` for primaries, `clauthHiddenAccounts` for clauth rows) and removes the `.claudeCode`/`.codex` primaries when hiding is on. **Postcondition: an empty clauth roster (no feed, schema ≠ 1, watcher not started) returns the primaries regardless of the flag** — the rail must never be empty. `ClauthVisibility.isShown(_:settings:)` is the predicate the Settings Order-row subtitle uses (1 hook line at `SettingsView.swift:363`). `enabledAccounts` is never touched.
- `Clauth/ClauthFetchGuard.swift` — `isClauthSlot(_:)`; `UsageStore.refresh()` filters `extras` through it (1 line) and `refresh(_:)` routes a clauth slot to the daemon `refresh` verb (2 lines); `UsageStore.applyClauth(_:)` is a 2-line method inside `UsageStore.swift` (`usage` is `private(set)`).
- Hooks (CLP-1 budget 32): `AppSettings`: `clauthAccounts` stored property with a `didSet` mirroring `extraAccounts.didSet` (`PanelMetrics.makeRoom(for: allAccounts.count)`; `onChange?()`) — 4; `allAccounts` append — 1; `label(for:)` clauth branch — 1; `shownAccounts` body — 1. `AppDelegate` — 2. `UsageStore` — 3 + `applyClauth` 2. `FloatingUsagePanelView` entries builder `preferring: settings.pinnedWindow(for: account) ?? ClauthMapping.defaultPin(for: account)` — 1. `SettingsView` Order-row subtitle — 1. Total ≈ 17; the rest is slack for what the compiler demands.
- Order: active claude, its chain order, other claude; then active codex, its chain, other codex — the default seed for the `clauth:` group in `orderedAccounts`; the user's drag order wins afterwards.
- Sandbox launch recipe (every bare launch, before the first one): `defaults write Pulse settings.launchAtLogin.decided -bool YES` (no LaunchAgent gets written) and `defaults write Pulse settings.enabledProviders -array claudeCode codex` (only the two primaries that hiding removes are enabled, so the rail is clauth-only and `wanted` is empty); the `Pulse` domain is the bare build's own, distinct from the installed app's. Assert `test ! -e ~/Library/LaunchAgents/com.pulse.launch-at-login.plist` before and after every run.
- Tests (`Tests/PulseTests`, `@testable import Pulse`): decode fixture — the executor BUILDS `Tests/PulseTests/Fixtures/status.json` with 7 profiles mirroring the live roster's shape (3 claude / 4 codex; names prefixed `fx-` so a ring named `fx-…` proves the sandbox is being read; one `session_feed`-only profile, one `fetch_status: RateLimited, stale: false`, one parked codex at 100% with a PAST `resets_at`, one claude with `7d fable` at 100% and `7d` at 12, the 2026-09 codex-limited shape with 1 banked reset); every mapping rule above as a table test incl. the drop rule, the 99.5 rule, a parked codex profile at 100% with a PAST `resets_at` asserting NOT exhausted, and `7d fable` at 100% asserting not exhausted; liveness ladder with injected `(generatedAtAge, statusMtimeAge)` pairs incl. fresh-mtime + stale-`generated_at` ⇒ `.live`; roster diff (added / removed / renamed); `ClauthVisibility.shown` over the 7-account roster ⇒ 7 clauth ids exactly once and the two primaries absent, a `providerOrder` interleaving a clauth id between two primaries survives, an EMPTY roster ⇒ the primaries return; `state == .stale` for the RateLimited profile and for a 2-hour-old `generated_at` with an equally old mtime; headline for the `7d fable` profile is 12%; `ClauthFetchGuard` ⇒ extras from the same roster is empty.

### CLP-2 — switch from the ring, the card knows the chain

Deliverable: a clauth ring offers "Switch to <name>" / "Refresh" / "Re-authenticate…" / "Rename…" / "Hide from rail"; the hover card shows active-slot state, chain position, threshold, `rotates at the session boundary`, `1 free reset banked`, and — when the daemon is dead — "clauth daemon not running · last reading <ago>"; a switch is reported settled only when the harness's OWN active slot flips (settle ladder), in-flight state visible on the ring.

- **Step 1, a 15-minute spike, recorded in `.agent/DECISIONS.md` with a screenshot path**: attach a two-item `.contextMenu` to one ring and right-click it on the sandboxed bare build with Liquid Glass off and on. If the menu presents, keep it. **Fallback decided now**: `FloatingPanel.sendEvent` gains `case .rightMouseDown where popUpClauthMenu(event): return` (1 hook line, costed) whose implementation in `Clauth/` maps the point to a ring through the existing `onClick` hit-testing path and calls `NSMenu.popUpContextMenu`.
- `Clauth/ClauthCLI.swift` — THE only file that spawns processes: resolves the binary as `environment["PULSE_CLAUTH_BIN"] ?? clauthBinary()`; **when `PULSE_CLAUTH_HOME` is set and `PULSE_CLAUTH_BIN` is not, every spawn is refused** with `.unreachable` reason "sandboxed" — and `launchctl` goes through the same door.
- `Clauth/ClauthDaemonClient.swift` — port of ccsbar `DaemonClient` (socket first, CLI fallback for switch via `ClauthCLI`, reply classification). Socket path = `${PULSE_CLAUTH_HOME}/clauthd.sock`. Test seam: injectable `send`.
- `Clauth/ClauthSwitchMachine.swift` — port of `SwitchMachine` + `StatusModelSwitch` verbatim, all three legs: (1) `.arming` — entered when the CURRENT account of that harness has a live session; ccsbar asks for a confirm and times out after 5s; **Pulse's surface for the confirm is an `NSAlert` raised by the ring-menu action** ("<name> has a live session — switching logs it out. Switch anyway?"; the accessory app activates for the alert), an unconfirmed arm fires nothing; (2) `.pending` → confirmed when the harness's OWN active slot flips in status.json (`pending_switch` extends the wait), timeout otherwise; (3) the CLI-confirm leg (`confirmedByCLI` when the socket was unreachable and `ClauthCLI` ran `clauth switch`).
- `Clauth/ClauthRingMenu.swift` — the menu content + a pure decision table (active row has no "Switch to"; `auth_status == broken` row leads with "Re-authenticate"; third-party rows have no reauth; every row has "Hide from rail" → `clauthHiddenAccounts`).
- `Clauth/ClauthCardFooter.swift` — the card's clauth block: active/chain line, the codex verdict line with ccsbar's attribution (`CodexStrip.rateLimitLine`: named window → that window; bare reason → the live window at ≥100%, else "is rate-limited"; lapsed → nothing), the banked-reset line, and `footnote(for:)` (daemon-dead wording "clauth daemon not running · last reading <ago>"). The `footnote` hook turns upstream's implicit `switch` into `return switch` — 2 lines. Hooks: `UsageDockItem` `.modifier(ClauthRingMenuModifier(account:))` — 1; `UsageDetailCard` body appends `ClauthCardFooter(account:)` — 1, and its `footnote` starts with `if let f = ClauthCardFooter.footnote(for: usage) { return f }` — 1; the panel fallback — 1 if needed. **No rail caption**: the profile name lives in the hover card title (already `settings.label(for:)`), the ring's accessibility label, and per-account tints (Pulse's own setting); names ON the rail are a CLP-4 candidate after AX has lived with it (`DockLayout` budget work).
- Tests: decision table; settle ladder port (incl. the wrong-harness case: claude switch, only `active_codex_profile` flips ⇒ NOT confirmed until the pending timeout; an armed switch with no confirm fires nothing; a confirmed one dispatches); reply classification; `ClauthCLI` refusal under sandbox (the injected spawn closure is never reached); `FakeClauthDaemon` (a Unix socket under the sandbox that records commands and mutates the fixture) used by the switch tests.

### CLP-3 — the control pane, ccsbar parity

Deliverable: Settings → sidebar "clauth" pane with everything ccsbar's panel does, and Pulse installed in `/Applications` from the fork build.

- `Clauth/ClauthSettingsPane.swift` (+ `ClauthChainEditor.swift`, `ClauthProxyRow.swift`, `ClauthTokenRow.swift`, `ClauthAddAccount.swift`, `ClauthAccountPane.swift`): daemon liveness + version header; **chain-global section** (wrap-off, weekly line `weekly_switch_threshold`, `burn_aware`, forecast — one each, they describe the claude chain); **per harness**: active account, chain rail with add/remove/move/threshold/last-resort/member-weekly(null clears)/check-weekly/check-scoped; codex proxy-mode toggle (port `CodexProxyMode` with `PULSE_CODEX_HOME` threaded as `configPath`, backup file `config.toml.bak-pulse`, `launchctl` via `ClauthCLI`); rolling-token install/replace/clear per claude profile (install = `login --setup-token --yes` with the token on stdin via `ClauthCLI`; the flag is read from status.json, never from the sidecar file); add account (capture / browser, claude / codex), rename (socket), delete (CLI, ccsbar's confirm shape); show/hide of Pulse's own primaries.
- `Clauth/ClauthAccountPane.swift` — Pulse's per-account pane for a `clauth:` slot: ring tint, pinned window, hide-from-rail, a link to the clauth pane — and NO Sign out / rename field / Remove account. Hook: one guard at the top of `accountPane` routing `clauth:` slots here.
- Hooks (CLP-3 budget 56): `SettingsPane` `case clauth` + title/symbol — 3; `row(_:)` icon switch `case .clauth` — 1; sidebar `row(.clauth)` — 1; detail `switch` `case .clauth:` — 1; `accountPane` guard — 2. VoiceOver/accessibility strings for clauth rows are built in a property first (upstream `CLAUDE.md` rule), never a conditional inside a `localized(...)` call.
- After install: automatic updates OFF (`defaults write io.github.qunqin24.Pulse SUEnableAutomaticChecks -bool false`, read back) — the baked feed is upstream's and would replace the fork; this is both an acceptance line and the closeout's first manual action.
- Parity checklist `Docs/clauth/PARITY.md`: every `func` in ccsbar `StatusModelActions.swift`, the `CodexProxyRow` switch, `SessionToken`'s install/replace/clear, checked off with the landing commit or a written reason it is not done (no empty rows).
- `Scripts/bundle.sh` → `build.noindex/Pulse.app` → `/Applications/Pulse.app`; ccsbar keeps running (retirement is CLP-4).

### CLP-4 — retire ccsbar (human-gated, NOT in this goal)

After AX has lived on the Pulse rail: README pointer in ccsbar, login item off, `/Applications/ccsbar.app` removed, ccsbar repo archived; rail captions if wanted. Needs AX's say-so — it deletes a working tool.

## Verification (the canonical command)

`CLP_HOOK_BUDGET=<n> Scripts/clauth-verify.sh` (frozen, ours; `make test` runs it — the spelling the autonomous-grind clear hook recognises) runs in order, failing on the first red: `swift package clean` + Swift 6 build with warnings-as-failure · `swift test` (exit status AND any per-suite failure count; fails on 0 tests) · `Scripts/check-localization.sh` · `Scripts/clauth-hook-budget.sh $CLP_HOOK_BUDGET` · gate 5: credential-file / Keychain / `~/.claude/settings.json` names in OUR code (`Sources/Pulse/Clauth`, `Tests`, and every added hook line in upstream files; comments allowed, anchored so `://` never counts as a comment) · gate 6: process spawns (`Process(`, `launchedProcess`, `NSTask`, `posix_spawn`, `NSWorkspace…open/launch`) in that same scope only inside `ClauthCLI.swift` · gate 7: no edits to release machinery / brand assets. Gates 5/6 deliberately do not scan upstream's own code — it reads the CLIs' credentials for its primary accounts and spawns codex, which is theirs.

## Safety rails (死规矩)

- Pulse reads `status.json`, `tokens.json`, `${PULSE_CODEX_HOME:-~/.codex}/config.toml`, and the socket. It NEVER reads `~/.clauth/profiles/*/credentials.json`, `codex-auth.json`, `session-token*`, `~/.claude/.credentials.json`, the Keychain, or `~/.codex/auth.json`. A clauth account is never a Pulse `ExtraAccount` with copied tokens. (Gate 5 of the verify script.)
- Every process spawn goes through `ClauthCLI.swift` (gate 6), which refuses to spawn under `PULSE_CLAUTH_HOME` unless `PULSE_CLAUTH_BIN` points at the sandbox's recording shim. During development the executor never sends `switch`, `rename`, `delete`, `fallback_*`, `set_*`, `login`, `rolling-token`, or `launchctl` to the REAL daemon/system: all interactive verification runs with `PULSE_CLAUTH_HOME=<sandbox> PULSE_CLAUTH_BIN=<sandbox>/bin/clauth PULSE_CODEX_HOME=<sandbox>/codex` against the fixture, the fake socket, and the shim. Read-only smoke against the real daemon is allowed: reading `~/.clauth/status.json`, `{"cmd":"snapshot"}`. `~/.clauth/daemon.log` must show no switch/rename/delete line stamped inside the run window.
- Live switches and browser OAuth on AX's real accounts are AX-manual (project rule) — the closeout lists them as the first thing AX does after install.
- No edits to `.github/workflows/*`, `appcast.xml`, `Scripts/appcast.py`, `Scripts/dmg.sh`, `Scripts/bundle.sh`, `VERSION`, `CHANGELOG.md` (gate 7); no tags; no edits to `Scripts/clauth-*.sh`.
- Sandbox runs use the BARE build (`swift build` product), never `/Applications/Pulse.app`: Sparkle is inert there and UserDefaults land in the `Pulse` domain, not the installed app's. The launch recipe (CLP-1) pre-seeds `settings.launchAtLogin.decided` so no LaunchAgent is ever written by a sandbox run, and enables only `claudeCode`+`codex` so no native provider fetches.
- Never connect Pulse's Claude status line (`StatusLineHook.install`) — `~/.claude/settings.json` is not ours to write, on the real machine or in a test.
- Under the sandbox `ClauthCLI.run(program:args:)` execs `$PULSE_CLAUTH_BIN` with `[program] + args` for EVERY program (clauth AND `/bin/launchctl`), so the shim records the intent and nothing real runs. During the real-daemon read-only smoke a ring click's `refresh` verb is allowed (it only re-fetches usage); nothing else is sent.
- The real-machine mutation oracle is a before/after snapshot, not the daemon log (it does not log rename/delete and it logs its own auto-switches): the sorted profile-name set from `~/.clauth/status.json`, `ls ~/.clauth/profiles | sort`, and both active slots, captured at run start and at closeout — identical sets, and any active-slot change explained by a `clauth daemon: … switched to` line.

## Known debts, not in this run

- clauth `docs/ccsbar/DESIGN.md` § socket was missing three verbs until `fa29c21` (fixed in this planning pass).
- PR #69 (upstream clauth) publishes `codex_wrap_off`; the deployed fork daemon does not. The pane treats wrap-off as global until the fork syncs.
